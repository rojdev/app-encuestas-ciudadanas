import "flowbite/dist/flowbite.min.css";
// Importa el JavaScript solo si necesitas la interactividad de los componentes
import { initFlowbite } from "flowbite";

export default ({ app }) => {
  // Inicializa Flowbite (opcional)
  initFlowbite();
};
